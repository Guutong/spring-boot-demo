image: java:8-jdk

stages:
  - build

job_build:
  stage: build
  script:
    - ./gradlew clean build
  artifacts:
    paths:
    - demo/build/libs/*.jar
    - demo/build/libs/*.jar
  only:
    - master
  tags:
    - java
    - docker